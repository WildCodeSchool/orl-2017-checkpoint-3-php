{% extends 'base.html.twig' %}
{% block body %}

    <div class="form-group">
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="update"/>
    {{ form_end(form) }}
    </div>

{% endblock %}